// The status is set to 'finished in X.Xs' and then the 'show changes' button is clicked, which
// reloads the page. It's useful to keep the status displayed across this page reload, but we
// don't want to always retain the status.
export function statusStoredForPageReload() {}
